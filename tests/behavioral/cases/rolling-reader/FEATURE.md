# Amount reader

Status: active.

Read the new `amount` message field while continuing to accept messages from the currently deployed writer during rollout.
The current writer is in `writer.py` and deploys independently.
